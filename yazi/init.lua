require("full-border"):setup{
  -- Border types come from Yazi's UI API
  -- Options include PLAIN, ROUNDED, DOUBLE, THICK, etc.
  type = ui.Border.ROUNDED,
  -- optional: tweak style, e.g. fg/bg (depends on your theme)
  -- style = { fg = "blue" },
}
